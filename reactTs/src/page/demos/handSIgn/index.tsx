/** 手写签名组件 */
import { useEffect } from 'react';
import Sketchpad from './sketchpad';

const HandSIgn = () => {

  // 初始化
  useEffect(() => {

  }, []);

  return (
    <div>
      <Sketchpad/>
    </div>
  );
};

export default HandSIgn;